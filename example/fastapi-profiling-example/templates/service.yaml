apiVersion: v1
kind: Service
metadata:
  name: {{ include "fastapi-profiling-example.fullname" . }}
  labels:
    {{- include "fastapi-profiling-example.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fastapi-profiling-example.selectorLabels" . | nindent 4 }}
